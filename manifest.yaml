# Dotfiles manifest — stow packages + skills wiring.
# Brew lives in Brewfile (canonical).

stow:
  - zsh
  - git
  - ghostty
  - opencode

zsh_plugins:
  dir: ~/.zsh/plugins
  repos:
    - https://github.com/zsh-users/zsh-autosuggestions
    - https://github.com/zsh-users/zsh-syntax-highlighting

skills:
  agent: opencode
  custom_dir: skills/custom
  agents_dir: ~/.agents/skills
  opencode_skills_dir: ~/.config/opencode/skills
  # skills.sh packages (source → skill names). Edit here; ./install --skills-only
  packages:
    JuliusBrussee/caveman:
      - caveman
    mattpocock/skills:
      - grill-me
      - grilling
      - grill-with-docs
    vercel-labs/agent-browser:
      - agent-browser

brewfile: Brewfile

secrets:
  path: ~/.config/secrets/env
  example: env.example

opencode:
  dir: ~/.config/opencode
